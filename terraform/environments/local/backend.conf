# Backend configuration for development environment
# Storage account and container should be created manually or via a separate bootstrap process

resource_group_name  = "rg-terraform-state-dev"
storage_account_name = "stcmhtfstatedev001"
container_name       = "terraform-state"
key                  = "azure-functions-java-demo/local/terraform.tfstate"
use_azuread_auth     = true