# Backend configuration for production environment
# Storage account and container should be created manually or via a separate bootstrap process

resource_group_name  = "rg-terraform-state-prod"
storage_account_name = "stcmhtfstateprd001"
container_name       = "terraform-state"
key                  = "azure-functions-java-demo/prod/terraform.tfstate"
use_azuread_auth     = true